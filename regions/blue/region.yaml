#
# region.yaml — blue region overrides
#
# "blue" is a non-AWS region identifier used for on-premises or non-standard environments.
# Merged as the second layer: default.yaml → THIS FILE → clusters/<type>/common.yaml → clusterdef.yaml
#

ldapServer: BLUE_LDAP
