#
# region.yaml — us-east-1 region overrides
#
# Merged as the second layer: default.yaml → THIS FILE → clusters/<type>/common.yaml → clusterdef.yaml
# Add keys here that vary by AWS region (LDAP endpoints, DNS zones, proxy config, etc.)
#

ldapServer: US_EAST_1_LDAP
