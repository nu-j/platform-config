#
# region.yaml — green region overrides
#
# "green" is a non-AWS region identifier used for on-premises or non-standard environments.
# Merged as the second layer: default.yaml → THIS FILE → clusters/<type>/common.yaml → clusterdef.yaml
#

ldapServer: GREEN_LDAP
